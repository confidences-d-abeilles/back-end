kind: pipeline
type: docker
name: CI

steps:
  - name: build
    image: docker
    commands:
      - yarn build
    volumes:
      - name: docker
        path: /var/run/docker.sock
  - name: test
    image: docker
    commands:
      - yarn build
      - yarn test
    volumes:
      - name: docker
        path: /var/run/docker.sock

volumes:
  - name: docker
    host:
      path: /var/run/docker.sock
