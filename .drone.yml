kind: pipeline
type: docker
name: default

steps:
  - name: build
    image: docker
    commands:
      - ls
    volumes:
      - name: docker
        path: /var/run/docker.sock

volumes:
  - name: docker
    host:
      path: /var/run/docker.sock
